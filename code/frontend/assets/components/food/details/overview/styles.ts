import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    overviewWrapper: {
      flexDirection: "row",
      padding: 20,
      borderTopWidth: 1,
      borderBottomWidth: 1,
      borderColor: "#dadada",
    },
  });
  