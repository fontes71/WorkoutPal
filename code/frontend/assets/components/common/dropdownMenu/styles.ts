import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    dropdown: {
      borderColor: 'gray',
      width: '30%',
      borderWidth: 0.5,
      borderRadius: 8,
      paddingHorizontal: 8,
    },
    selectedTextStyle: {
      fontSize: 16,
    },
  });

  export default styles